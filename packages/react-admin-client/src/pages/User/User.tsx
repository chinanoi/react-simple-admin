import React from 'react';
import { Link } from 'react-router-dom';
import './User.scss';

const User = () => {
    return (
        <div className="userList">
            用户管理
        </div>
    );
};

export default User;
