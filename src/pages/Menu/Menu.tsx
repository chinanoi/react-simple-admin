import React from 'react';
import { Link } from 'react-router-dom';
import './Menu.scss';

const Menu = () => {
    return (
        <div className="userList">
            菜单管理
        </div>
    );
};

export default Menu;
