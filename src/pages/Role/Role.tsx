import React from 'react';
import { Link } from 'react-router-dom';
import './Role.scss';

const Role = () => {
    return (
        <div className="userList">
            角色管理
        </div>
    );
};

export default Role;
